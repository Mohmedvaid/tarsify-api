/**
 * TEMP: Platform Initialization Routes
 * For initial seeding of RunPod endpoints and base models
 *
 * ⚠️ REMOVE THIS AFTER INITIAL SETUP ⚠️
 * These routes bypass normal admin auth and use a simple token.
 * Delete this entire folder once endpoints/base-models are seeded.
 */
export { initRoutes } from './init.routes';
